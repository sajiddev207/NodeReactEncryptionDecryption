# NodeReactEncryptionDecryption

# Secret key that used in node code and react code must be same.

# Install node modules in both project 

npm i || npm i --force

# crypto-js
For encryption and decryption we use 'crypto-js' library